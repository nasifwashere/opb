export const data = { name: 'set', description: 'Set the bot\'s main channel.' };

// Stub for server setup
export async function execute(message, args, client) {
  message.reply('Main channel set (stub).');
}